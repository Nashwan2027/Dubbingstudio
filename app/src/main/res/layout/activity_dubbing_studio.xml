<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".ui.studio.DubbingStudioActivity">

    <!-- ✅ Toolbar الرئيسي للقائمة -->
    <com.google.android.material.appbar.MaterialToolbar
        android:id="@+id/toolbar"
        android:layout_width="0dp"
        android:layout_height="?attr/actionBarSize"
        android:background="@color/colorPrimary"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:title="استوديو الدبلجة"
        app:titleTextColor="@android:color/white"
        app:navigationIcon="@drawable/ic_arrow_back" />

    <!-- ✅ شريط الأدوات أسفل الـ Toolbar الرئيسي -->
    <LinearLayout
        android:id="@+id/top_toolbar"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:padding="8dp"
        android:background="@color/surface"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/toolbar">

        <!-- زر المزامنة الذكية -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/btn_smart_sync"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:layout_marginEnd="4dp"
            android:text="🔄 مزامنة"
            android:textSize="12sp"
            style="?attr/materialButtonOutlinedStyle"
            app:icon="@drawable/ic_sync"
            app:iconGravity="textStart" />

        <!-- زر إضافة بطاقة -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/btn_add_card"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:layout_marginEnd="4dp"
            android:text="➕ إضافة"
            android:textSize="12sp"
            style="?attr/materialButtonOutlinedStyle" />

        <!-- زر التصدير -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/btn_export_final"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="💾 تصدير"
            android:textSize="12sp" />

    </LinearLayout>

    <!-- شريط التقدم -->
    <ProgressBar
        android:id="@+id/progressBar"
        android:layout_width="0dp"
        android:layout_height="4dp"
        android:visibility="gone"
        style="?android:attr/progressBarStyleHorizontal"
        app:layout_constraintTop_toBottomOf="@id/top_toolbar"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

    <!-- قائمة البطاقات -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recycler_view_dialogues"
        android:layout_width="0dp"
        android:layout_height="0dp"
        tools:listitem="@layout/list_item_dialogue_card"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/progressBar" />

</androidx.constraintlayout.widget.ConstraintLayout>